# create-svelte-tv-app

Create a Svelte TV app with TypeScript, routing, ESLint, and Prettier.

```sh
pnpm create svelte-tv-app
```

The generated project includes a small two-route demo app and a Canvas renderer
setup that works without generated SDF fonts.
